# Foot-aware spatial encoder v7 — approved first experiment

Mode: direct bounded implementation of the user's approved first experiment, not a Ralplan consensus/receipt claim. User approved proceeding after the literature recommendation. Native executor creation is thread-limit blocked; leader owns implementation, existing explore agent provides installed API facts. No dependencies, remote operations, commits, real robot work, or changes to v0-v6.

Scope: add separate v7 tasks and a feed-forward CNN policy. Hold terrain physics, rewards, training family weights, action8 effort scale7.5, episode limits, and strict13.1/53.1m crossing criteria unchanged. Defer GRU, residual policy, CoP/contact rewards and adaptive curriculum so this test is attributable. Existing v5 remains recommended unless fresh gate passes.

Observation: old60 prefix +221 height +221 validity +four distal-foot centers xyz relative torso yaw =514D. Shared17x13 raygrid .2m at offsetx.4 coversx[-1.2,2],y[-1.2,1.2], extending v6 rear/side coverage to represent four feet. ordering=xy implies13rows(y),17columns(x). All variants share this same scene. Ideal raycast/analytic plane, no real depth camera/occlusion/family oracle. Foot positions derive articulation kinematics, not terrain metadata. Gaussian foot maps(sigma.15m) are policy-internal, four channels, outside/invalid centers yieldzero (no edge-clamping).

Policy: ActorCritic compatible with installed RSL-RL3.0.1. Sixchannel smallCNN->32embedding, then60+32 ->400/200/100 ->8action (separatecritic). Three checkpoint-persisted modes: blind(allterrain/foot channelszero), height(height+valid only), footmap(all6channels). Same architecture/PPO budgets. Copy old60Dactor/critic weights, zero new32firstlayercolumns, resetstd.20/optimizer/iteration; exact initial deterministic actions required. No frozen/residual policy yet.

Pretraining gates: full oldtests; math grid order, yaw/translation invariance, fourfootorder/offset, invalid/offgrid map, modes invariance, strict checkpointsave/load, gradientflow, exact realparentactions. Simulator probe checks actualraystarts matchpolicygrid, foottransforms withnonzero yaw/roll, finite514D, reset/wrapfreshness;64env2itertraining+evaluation.

Predeclared experiment: variants blind,height,footmap x trainingseed42/43/44, geometryseed51/58/59 respectively (matched withinseed),4096env*32steps*750iterations each=98,304,000transitions/run. LR1e-4 fixed,gamma.995,entropy.002,std.20,5epochs/4minibatches. Use finaliteration749 ONLY, not bestvalidationcheckpoint. Existing v5 can be encoded usingzero-terrainweights for frozenreference. Benchmark all9finals +frozenreference on held-outgeometry60/reset34 and61/reset35,175env each. These conditions are untouched before finalcheckpointfreeze. Selected representative seed42 predetermined, not scorepicked. All trainingseeds/failures reported, samegeometry perpairedarm, randomstreams maydiverge underdifferentpolicyactions.

Promotiongate: footmap pooled strictone >=bothtrainedcontrols, six>=bothcontrols, falls<=bothcontrols, zero worldexit; majority(>=2/3)seedpairs strictone improvement overheight; also no aggregate regression vsfrozenv5 onone/six/falls/flatfalls. Report0.8/1.0 eachfamily withdenominators; finite samples/singlegeometrypertrainseed notuniversalclaim. No adaptation afterheldout. Diagnostic depthzero /footmap-disabled sweeps only after frozen scores, cannot establish sensor-onlycausality.

Deliver selfdescribing checkpoints+SHA/config/commandlogs, report and independently recomputed rawarray metrics, onefixed hardterrain qualitativevideo pair if renderer works, preserved failures. Verify tests/compile/shell/diff; disclose unavailablelint/typechecktools. Ifsmoke findsintegrationerrors fixbeforetraining; ifresource costforcesbudgetchange recordbeforeanyarm'sfullrun, never unequalbudgets. No need forperformancepromotion to honestly complete experiment; report failure ratherthan hide it.

## Resource routing evidence (before full runs)
GPU0 throughputprobe: ~5.4s/iteration under concurrent unrelated desktop aeirobot_sim RTX renderer (PID930479) using GPU0. The renderer belongs to the user's other app and is NOT stopped/paused. Route this experiment to otherwiseidle GPU1(RTX5070); explicit both --device cuda:1 and agent.device=cuda:1 are necessary because the inherited CLI does not change agentdevice automatically. At most one experimentGPUjob, no parallel trainingjobs. NewGPU1 throughputprobe is excluded fromtraining/evaluation.
